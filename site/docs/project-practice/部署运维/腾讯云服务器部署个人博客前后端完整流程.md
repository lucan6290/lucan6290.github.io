---
title: 腾讯云服务器部署个人博客前后端完整流程
date: '2026-07-07T15:00:54+08:00'
last_update:
  date: '2026-07-08T15:35:51+08:00'
  author: lucan
description: 详细记录个人博客前端+管理端前后端项目在腾讯云CVM服务器的从零到一部署全流程，包含服务器初始化、环境搭建、项目上传、配置代理、开机自启、域名访问等操作，可直接复刻用于新服务器部署。
sidebar_position: 2
---

**部署目标**

你的文章全部开源，所以博客本身可以公开访问。需要保护的是：

- 管理前端 `/admin/`
- 管理后端 `/api/`
- 服务器 SSH
- 后端管理员账号密码

最终部署结构：

```text
GitHub 仓库
  -> 保存项目源码与文章内容

腾讯云服务器 43.136.136.167
  -> 手动拉取 GitHub 仓库代码
  -> 手动构建博客前端
  -> 手动构建管理前端
  -> systemd 运行管理后端
  -> Nginx 对外提供访问
```

当前没有域名，因此线上访问形态为：

```text
http://43.136.136.167/          公开博客
http://43.136.136.167/admin/   管理前端，需 Nginx 访问密码 + 应用登录
http://43.136.136.167/api/     管理后端，仅 Nginx 转发，不直接暴露端口
```

---

**一、上线前准备**

1. 当前服务器信息：

```text
服务器公网 IP：43.136.136.167
服务器登录用户：ubuntu
SSH 端口：22
操作系统：Ubuntu 24.04 LTS
项目部署目录：/home/ubuntu/lucan/xiaocancoding/app
```

2. GitHub 分支策略：

```text
develop：服务器手动拉取的部署分支
main：公开稳定分支，可用于后续 GitHub Pages 或正式发布
```

当前先不配置 CI 自动部署。服务器更新方式为：

```text
登录服务器
进入项目目录
git pull 拉取最新代码
重新构建前端
更新后端依赖
重启后端服务
重载 Nginx
```

3. 当前没有域名，因此暂时不配置 HTTPS。

后续如果购买域名，需要：

```text
域名 A 记录 -> 43.136.136.167
Nginx server_name 改成真实域名
后端 CORS 配置改成 https://真实域名
再执行 certbot 配置 HTTPS
```

---

**二、服务器初始化**

登录服务器：

```bash
ssh ubuntu@43.136.136.167
```

![image](./腾讯云服务器部署个人博客前后端完整流程-imgs/腾讯云服务器部署个人博客前后端完整流程-imgs-1.png "腾讯云服务器部署个人博客前后端完整流程-imgs-1.png")

安装基础组件：

```bash
sudo apt update
sudo apt install -y nginx git curl rsync apache2-utils ufw
```

![image](./腾讯云服务器部署个人博客前后端完整流程-imgs/腾讯云服务器部署个人博客前后端完整流程-imgs-2.png "腾讯云服务器部署个人博客前后端完整流程-imgs-2.png")

![image](./腾讯云服务器部署个人博客前后端完整流程-imgs/腾讯云服务器部署个人博客前后端完整流程-imgs-3.png "腾讯云服务器部署个人博客前后端完整流程-imgs-3.png")

安装 Node.js 20：

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
node -v
npm -v
```

安装 Python 3.10 管理工具 `uv`：

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.profile
uv python install 3.10
```

![image](./腾讯云服务器部署个人博客前后端完整流程-imgs/腾讯云服务器部署个人博客前后端完整流程-imgs-4.png "腾讯云服务器部署个人博客前后端完整流程-imgs-4.png")

创建部署目录：

```bash
mkdir -p /home/ubuntu/lucan/xiaocancoding
sudo chown -R ubuntu:ubuntu /home/ubuntu/lucan
```

检查目录：

```bash
ls -ld /home/ubuntu/lucan
ls -ld /home/ubuntu/lucan/xiaocancoding
```

![image](./腾讯云服务器部署个人博客前后端完整流程-imgs/腾讯云服务器部署个人博客前后端完整流程-imgs-9.png "腾讯云服务器部署个人博客前后端完整流程-imgs-9.png")

---

**三、服务器安全策略**

防火墙只开放必要端口：

```bash
sudo ufw allow OpenSSH
sudo ufw allow 80
sudo ufw allow 443
sudo ufw enable
sudo ufw status
```

![image](./腾讯云服务器部署个人博客前后端完整流程-imgs/腾讯云服务器部署个人博客前后端完整流程-imgs-6.png "腾讯云服务器部署个人博客前后端完整流程-imgs-6.png")

不要开放这些端口：

```text
18000  后端端口，只监听 127.0.0.1
14000  本地开发端口，线上不用开放
3400   本地开发端口，线上不用开放
```

管理后端真实运行在：

```text
127.0.0.1:18000
```

只能由 Nginx 访问，公网不能直接访问。

---

**四、拉取项目代码**

进入部署目录：

```bash
cd /home/ubuntu/lucan/xiaocancoding
```

首次部署时克隆仓库：

```bash
git clone -b develop https://github.com/lucan6290/lucan6290.github.io.git app
```

进入项目目录：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app
```

确认当前分支：

```bash
git branch
```

确认远程仓库：

```bash
git remote -v
```

后续更新代码时，不再重新 clone，只需要：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app
git checkout develop
git pull origin develop
```

---

**五、配置管理端访问密码**

管理端采用两层保护：

1. Nginx Basic Auth：进入 `/admin/` 和 `/api/` 前先输浏览器访问密码。
2. 项目自己的后台登录：`LUCHUAN_ADMIN_USERNAME` / `LUCHUAN_ADMIN_PASSWORD`。

创建 Nginx 访问密码：

```bash
sudo htpasswd -c /etc/nginx/.htpasswd_xiaocan lucan
```

执行后会要求输入密码。这里输入的是浏览器访问 `/admin/` 时弹窗使用的密码。

最终浏览器弹窗账号为：

```text
用户名：lucan
密码：执行 htpasswd 时你自己输入的密码
```

![image](./腾讯云服务器部署个人博客前后端完整流程-imgs/腾讯云服务器部署个人博客前后端完整流程-imgs-7.png "腾讯云服务器部署个人博客前后端完整流程-imgs-7.png")

---

**六、配置后端环境变量**

进入后端目录：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app/admin/backend
```

生成后端 token 签名密钥：

```bash
openssl rand -hex 32
```

这个值用于 `LUCHUAN_AUTH_SECRET`。它不是登录密码，而是后端用来签名登录 token 的密钥。

创建 `.env` 文件：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app/admin/backend

read -s -p "请输入管理后台应用登录密码：" ADMIN_PASSWORD
echo
AUTH_SECRET=$(openssl rand -hex 32)

cat > .env <<EOF
LUCHUAN_ENV=prod
LUCHUAN_PROJECT_ROOT=/home/ubuntu/lucan/xiaocancoding/app
LUCHUAN_LOG_LEVEL=INFO
LUCHUAN_CORS_ORIGINS=http://43.136.136.167
LUCHUAN_ADMIN_USERNAME=lucan
LUCHUAN_ADMIN_PASSWORD=${ADMIN_PASSWORD}
LUCHUAN_AUTH_SECRET=${AUTH_SECRET}
LUCHUAN_AUTH_TOKEN_TTL_MINUTES=720
EOF

chmod 600 .env
```

检查 `.env` 是否存在：

```bash
ls -l .env
```

不要把 `.env` 提交到 Git 仓库。

---

**七、安装后端依赖**

进入后端目录：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app/admin/backend
```

创建 Python 3.10 虚拟环境：

```bash
/home/ubuntu/.local/bin/uv venv --python 3.10 .venv
```

激活虚拟环境：

```bash
source .venv/bin/activate
```

安装依赖：

```bash
/home/ubuntu/.local/bin/uv pip install -r requirements.txt
```

---

**八、构建博客前端**

进入博客前端目录：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app/site
```

安装依赖：

```bash
npm ci
```

构建静态文件：

```bash
npm run build
```

构建完成后，博客静态文件会生成在：

```text
/home/ubuntu/lucan/xiaocancoding/app/site/build
```

---

**九、构建管理前端**

进入管理前端目录：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app/admin/frontend
```

安装依赖：

```bash
npm ci
```

构建管理前端：

```bash
VITE_API_BASE_URL=/api npm run build
```

构建完成后，管理前端静态文件会生成在：

```text
/home/ubuntu/lucan/xiaocancoding/app/admin/frontend/dist
```

其中：

```bash
VITE_API_BASE_URL=/api
```

非常重要。它表示管理前端上线后通过当前站点的 `/api` 访问后端，而不是访问浏览器本机的 `127.0.0.1:18000`。

---

**十、配置后端 systemd 服务**

`systemd` 用来让管理后端常驻运行，并在服务器重启后自动启动。

创建服务文件：

```bash
sudo tee /etc/systemd/system/xiaocan-admin-backend.service >/dev/null <<'EOF'
[Unit]
Description=Xiaocan Coding Admin Backend
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/home/ubuntu/lucan/xiaocancoding/app/admin/backend
EnvironmentFile=/home/ubuntu/lucan/xiaocancoding/app/admin/backend/.env
ExecStart=/home/ubuntu/lucan/xiaocancoding/app/admin/backend/.venv/bin/python -m uvicorn scr.main:app --host 127.0.0.1 --port 18000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF
```

启用服务：

```bash
sudo systemctl daemon-reload
sudo systemctl enable xiaocan-admin-backend
```

启动后端服务：

```bash
sudo systemctl restart xiaocan-admin-backend
```

查看服务状态：

```bash
sudo systemctl status xiaocan-admin-backend
```

---

**十一、配置 Nginx**

规则：

- `/`：公开访问博客
- `/admin/`：需要 Basic Auth
- `/api/`：需要 Basic Auth，并反向代理到后端

创建 Nginx 配置：

```bash
sudo tee /etc/nginx/sites-available/xiaocancoding >/dev/null <<'EOF'
server {
    listen 80;
    server_name 43.136.136.167;

    root /home/ubuntu/lucan/xiaocancoding/app/site/build;
    index index.html;

    location / {
        try_files $uri $uri/ /404.html;
    }

    location = /admin {
        return 301 /admin/;
    }

    location /admin/ {
        auth_basic "Private Admin";
        auth_basic_user_file /etc/nginx/.htpasswd_xiaocan;

        alias /home/ubuntu/lucan/xiaocancoding/app/admin/frontend/dist/;
        try_files $uri $uri/ /admin/index.html;
    }

    location /api/ {
        auth_basic "Private API";
        auth_basic_user_file /etc/nginx/.htpasswd_xiaocan;

        proxy_pass http://127.0.0.1:18000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
EOF
```

启用配置：

```bash
sudo ln -sf /etc/nginx/sites-available/xiaocancoding /etc/nginx/sites-enabled/xiaocancoding
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx
```

---

**十二、当前不配置 HTTPS**

当前没有域名，因此先使用 HTTP：

```text
http://43.136.136.167/
http://43.136.136.167/admin/
```

后续如果有域名，再执行 HTTPS 配置。

域名解析生效后，将 Nginx 中的：

```nginx
server_name 43.136.136.167;
```

改为：

```nginx
server_name 你的真实域名;
```

然后执行：

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d 你的真实域名
```

同时后端 `.env` 中的：

```text
LUCHUAN_CORS_ORIGINS=http://43.136.136.167
```

改为：

```text
LUCHUAN_CORS_ORIGINS=https://你的真实域名
```

---

**十三、手动更新部署流程**

之后每次代码更新，不需要重新初始化服务器，只需要在服务器执行以下流程。

进入项目目录：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app
```

拉取最新代码：

```bash
git checkout develop
git pull origin develop
```

更新博客前端：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app/site
npm ci
npm run build
```

更新管理前端：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app/admin/frontend
npm ci
VITE_API_BASE_URL=/api npm run build
```

更新后端依赖：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app/admin/backend
source .venv/bin/activate
/home/ubuntu/.local/bin/uv pip install -r requirements.txt
```

重启后端：

```bash
sudo systemctl restart xiaocan-admin-backend
```

检查并重载 Nginx：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

完整更新流程可以合并为：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app
git checkout develop
git pull origin develop

cd /home/ubuntu/lucan/xiaocancoding/app/site
npm ci
npm run build

cd /home/ubuntu/lucan/xiaocancoding/app/admin/frontend
npm ci
VITE_API_BASE_URL=/api npm run build

cd /home/ubuntu/lucan/xiaocancoding/app/admin/backend
source .venv/bin/activate
/home/ubuntu/.local/bin/uv pip install -r requirements.txt

sudo systemctl restart xiaocan-admin-backend
sudo nginx -t
sudo systemctl reload nginx
```

---

**十四、正式发布流程**

推荐发布节奏：

```text
1. 本地开发
2. 提交代码到 GitHub
3. 登录腾讯云服务器
4. 在服务器执行 git pull
5. 重新构建博客前端
6. 重新构建管理前端
7. 更新后端依赖
8. 重启后端服务
9. 访问 http://43.136.136.167/ 验证博客
10. 访问 http://43.136.136.167/admin/ 验证管理后台
```

本地提交代码：

```bash
git checkout develop
git add .
git commit -m "更新博客内容"
git push origin develop
```

服务器更新代码：

```bash
ssh ubuntu@43.136.136.167
cd /home/ubuntu/lucan/xiaocancoding/app
git checkout develop
git pull origin develop
```

---

**十五、上线后检查**

服务器检查：

```bash
sudo systemctl status xiaocan-admin-backend
sudo journalctl -u xiaocan-admin-backend -n 100 --no-pager
sudo nginx -t
curl -I http://127.0.0.1
curl http://127.0.0.1:18000/api/v1/health
```

浏览器检查：

```text
http://43.136.136.167/
http://43.136.136.167/admin/
```

预期效果：

- 博客首页无需密码，公开可访问。
- `/admin/` 会先弹出 Nginx 访问密码。
- Basic Auth 用户名为 `lucan`。
- 进入后台后，还需要使用项目自己的管理员账号登录。
- 管理后台应用用户名为 `lucan`。
- `/api/` 不直接暴露公网端口，只能通过 Nginx 访问。
- 服务器公网不开放 `18000`、`14000`、`3400`。

---

**十六、常用排查命令**

查看后端服务状态：

```bash
sudo systemctl status xiaocan-admin-backend
```

查看后端日志：

```bash
sudo journalctl -u xiaocan-admin-backend -n 100 --no-pager
```

重启后端：

```bash
sudo systemctl restart xiaocan-admin-backend
```

检查 Nginx 配置：

```bash
sudo nginx -t
```

重载 Nginx：

```bash
sudo systemctl reload nginx
```

查看端口监听：

```bash
sudo ss -tulpn
```

确认后端只监听本机：

```bash
curl http://127.0.0.1:18000/api/v1/health
```

---

**十七、回滚方案**

最简单的回滚方式是在服务器回退 Git 提交。

进入项目目录：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app
```

查看提交记录：

```bash
git log --oneline -n 10
```

回退到指定提交：

```bash
git checkout develop
git reset --hard 目标提交ID
```

重新构建并重启：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app/site
npm ci
npm run build

cd /home/ubuntu/lucan/xiaocancoding/app/admin/frontend
npm ci
VITE_API_BASE_URL=/api npm run build

cd /home/ubuntu/lucan/xiaocancoding/app/admin/backend
source .venv/bin/activate
/home/ubuntu/.local/bin/uv pip install -r requirements.txt

sudo systemctl restart xiaocan-admin-backend
sudo nginx -t
sudo systemctl reload nginx
```

如果确认要让 GitHub 上的 `develop` 分支也回滚，建议在本地使用：

```bash
git revert 某个出问题的提交
git push origin develop
```

然后服务器再执行：

```bash
cd /home/ubuntu/lucan/xiaocancoding/app
git pull origin develop
```

---

**最终部署结果**

```text
腾讯云服务器:
  手动拉取 develop 分支代码
  公开博客：http://43.136.136.167/
  管理后台：http://43.136.136.167/admin/
  /admin/ 和 /api/ 使用 Nginx Basic Auth 保护
  管理后台应用账号 lucan
  后端只监听 127.0.0.1:18000
```

当前访问地址：

```text
公开博客：http://43.136.136.167/
管理后台：http://43.136.136.167/admin/
```
